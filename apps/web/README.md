# Web Application

The first application to implement: a mobile-first, responsive English interface for the QA AI Lab library and tool catalog.

Development should support local preview before committing or publishing. The published application should serve both phone and desktop browsers.

Use the shared content contract and localization resources described in [architecture](../../docs/architecture.md). This folder is a scaffold; no application or framework has been initialized yet.
