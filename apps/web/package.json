{
  "name": "qa-ai-lab-web",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild reader.js --bundle --format=esm --splitting --minify --outdir=assets --target=es2022"
  },
  "dependencies": {
    "dompurify": "3.4.15",
    "marked": "18.0.11",
    "mermaid": "11.17.2"
  },
  "devDependencies": { "esbuild": "0.28.2" }
}
